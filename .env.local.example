# Stability AI API Key for generating synthetic portraits
# Get your key from: https://platform.stability.ai/
STABILITY_API_KEY=

# Optional: Photo base URL (defaults to empty string for relative paths)
NEXT_PUBLIC_PHOTO_BASE=

# GoPro Demo API Configuration
# These credentials are used for silent token refresh on the server-side
# Copy this file to .env.local and fill in your demo credentials

# Demo GoPro username (optional - for silent retry on 401)
DEMO_USERNAME=bjarni

# Demo GoPro password (optional - for silent retry on 401)
DEMO_PASSWORD=Hugvit1

# Base URL for API calls (optional - defaults to http://localhost:3000)
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Note: If DEMO_USERNAME and DEMO_PASSWORD are not set, the API routes will
# not attempt silent token refresh on 401 errors. Users will be prompted to
# sign in again when their token expires.
